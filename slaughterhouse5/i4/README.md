========================================================================
| i4 — the I^4 identity root                                           |
========================================================================


**i4** is the **I-symbol to the 4th (I^4)**: the I-collapse across all four
planes, and the self-referential deep-operand root of the I-13 program. It is
the **root** element of the learning scope `{i, c, sa, ssa}`.

## What it does
- **Identity** — `I(x) = x` (idempotent); `i_am_i` is the "I am I" rule that
  the cortex's idempotence rule originates from.
- **I^4 collapse** — binds the identity seed to each of L1–L4 and folds them
  (the same hex-concat convention as `constructor`) into one self-referential
  root: `9e0afcab…` for the frozen spec.
- **Attest** — binds downstream `agent`/`subagent` to the same declared
  I-13 identity (the frozen spec sha).

## Grounded anchors (verbatim from `hermes.i13`)
- `FROZEN_SPEC_SHA = 64881ebf…` (I-13 v2 FROZEN)
- `CONSENSUS_ROOT  = cd459333…` (6-agent `.dlw` consensus)

## Usage
```bash
python -m i4.cli identity 7
python -m i4.cli collapse
python -m i4.cli attest 64881ebf502b87bb450f1f39b71066013e0c31a7f78dedcae326f6155ddc6bf8
pytest tests/
```

## Relationship to the other modules
`i4` seeds the biosphere (the PULSE's `seed` step) and is the identity every
`agent`/`subagent` attests to. It is the self-reference the other organs
derive from.

## Provenance
I-13 is human-directed (David Lee Wise) + AI-co-authored (Hermes Agent,
Nous Research). AI co-creator credit is provenance, not derivation.

========================================================================
